const age = prompt("Please, enter your age:");

if (age >= 18 && age <= 65) {
  console.log(`Welcome to our web-site!`);
  alert(`Welcome to our web-site!`);
} else {
  console.log(`Unfortunately access denied`);
  alert(`Unfortunately access denied`);
}
